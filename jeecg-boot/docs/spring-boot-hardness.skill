---
name: spring-boot-hardness
description: Spring Boot 代码任务的 Hardness 执行约束。用于需求分析、实现、验证、回滚与证据闭环；与 spring-boot-dev.skill 配套加载，要求先计划后实现并在每阶段留痕。
---

## Skill 触发与注入（强制）
- 当任务涉及 Spring Boot 代码（Controller/Service/Mapper/Entity/配置/SQL/启动入口）时，开始阶段必须同时加载：
  - `docs/spring-boot-dev.skill`
  - `docs/spring-boot-hardness.skill`
- 未声明分级时，默认按 `H2` 执行。
- 输出必须包含：完成项、未完成项、风险、证据索引。

# Hardness 文档编写规范（AI 编码执行约束）

版本：v1.0  
日期：2026-03-31  
适用范围：本仓库所有由 AI 参与的软件开发任务（需求分析、编码、测试、文档、发布）

---

## 1. 目标与定义

### 1.1 目标
`hardness` 文档的目标是把“任务描述”升级为“可执行、可验证、可追责”的执行约束，降低以下风险：
- 上下文窗口过长导致遗忘关键约束
- 任务规模过大导致漏项、跳步、假完成
- 只给出“看起来完成”的结果，但缺乏可复核证据

### 1.2 定义
- `Hardness 文档`：针对一次任务的执行规则文档，不是功能说明文档。
- `任务单元`：可独立验收的最小交付块，必须有输入、动作、输出、验收标准。
- `证据`：可复现的事实材料，例如测试命令与结果、文件路径、接口返回、截图、日志片段。

---

## 2. 编写原则（必须遵守）

1. `可验证优先`：每条要求必须能被测试、脚本、人工检查或日志验证。
2. `先计划后实现`：复杂任务必须先拆分计划，再编码，再验证。
3. `证据闭环`：结论必须对应证据；没有证据视为未完成。
4. `上下文治理`：文档必须明确长任务的上下文压缩与恢复策略。
5. `持续评估`：每次改动后都要有最小验证，不允许“最终一次性验证”。

---

## 3. Hardness 分级（按任务复杂度）

## 3.1 H1（微小改动）
适用：单文件、小逻辑、低风险。  
最低要求：
- 目标与边界
- 变更清单
- 至少 1 条验证命令或检查项

## 3.2 H2（常规需求）
适用：多文件改动或中等业务逻辑。  
最低要求：
- 任务拆分（>=3 个任务单元）
- 每个单元的验收标准
- 回归影响清单
- 失败回退方案

## 3.3 H3（跨模块/大任务）
适用：跨模块改造、长链路需求、多人协作。  
最低要求：
- 里程碑计划（阶段门禁）
- 上下文预算与压缩策略
- 风险矩阵（概率/影响/缓解）
- 每阶段证据归档

> 默认策略：未声明分级时，按 `H2` 执行。

---

## 4. 文档结构模板（强制）

每个 hardness 文档必须包含以下章节，不得省略。

## 4.1 元信息
- 任务 ID
- 任务名称
- 分级（H1/H2/H3）
- 负责人（人类/AI）
- 开始时间、目标完成时间
- 关联需求/工单/PR

## 4.2 目标与非目标
- 目标（最多 3 条，必须量化）
- 非目标（明确不做什么，防止范围蔓延）

## 4.3 输入约束
- 已知上下文（文档、代码路径、接口、配置）
- 强约束（技术栈、兼容性、安全、性能）
- 禁止事项（例如：不可跳过测试、不可改公共契约）

## 4.4 任务分解（核心）
每个任务单元必须包含：
- `输入`：依赖与前置条件
- `执行动作`：具体改动
- `输出`：产物文件/接口/行为变化
- `验收标准`：可判定通过/失败
- `证据类型`：命令结果、截图、日志、diff

## 4.5 验证矩阵
至少覆盖：
- 功能正确性
- 回归影响
- 边界/异常路径
- 性能或资源影响（如适用）
- 安全与权限（如适用）

格式建议：

| 验证项 | 方法 | 阈值/期望 | 结果证据 |
|---|---|---|---|
| 单元测试 | `npm test`/`mvn test` | 全绿 | 命令输出 |
| 关键接口回归 | Postman/curl | 状态码+字段一致 | 响应样例 |

## 4.6 上下文窗口与大任务防漏策略
必须写清：
- 上下文预算（按阶段分配）
- 分段执行策略（每段结束输出阶段总结）
- 压缩策略（保留：目标、未完成项、风险、下一步）
- 恢复策略（重启后如何恢复到正确状态）

## 4.7 风险与回退
- 风险项、触发条件、监控信号
- 回退路径（代码、配置、数据）
- 回退验证步骤

## 4.8 完成定义（Definition of Done）
必须全部满足：
- 代码改动已完成并自检
- 验证矩阵执行完毕且记录证据
- 未完成项显式列出（若无写“无”）
- 输出总结与后续建议

---

## 5. Docs 架构与内容分工（本仓库）

### 5.1 文档分层
- `AGENTS.md`：协作约束、开发硬规则、文档更新总原则。
- `PLANS.md`：任务计划、执行步骤、风险与过程记录。
- `docs/changelog.md`：对外行为变化留痕（面向发布与回溯）。
- `docs/decisions/*.md`：架构或策略取舍（ADR）。
- `docs/architecture.md`：模块边界、启动入口、运行架构说明。
- `docs/config/configuration.md`：环境配置矩阵与敏感配置规范。
- `docs/api/Index.md`：API 文档目录索引（仅目录，不承载接口规范）。
- `docs/api/*.md`：按业务域沉淀接口清单、参数和示例。

### 5.2 变更触发矩阵（强制）
| 变更类型 | 必更文档 | 说明 |
|---|---|---|
| Controller/API 行为变化 | `docs/api/*.md` + `docs/changelog.md` | 对外契约变更必须文档化并留痕 |
| 配置变更（`application*.yml/properties`） | `docs/config/configuration.md` + `docs/changelog.md` | 包含新增键、默认值、环境差异 |
| 架构/策略取舍 | `docs/decisions/*.md` | 需要写清背景、方案、权衡、影响 |
| 中大型任务执行 | `PLANS.md` + 对应 Hardness 文档 | 先计划再实施，执行过程可追溯 |
| 数据库结构/索引变更 | `PLANS.md` + `docs/changelog.md`（必要时补 ADR） | 必须说明影响范围与回滚路径 |

### 5.3 Hardness 与 docs 的关系
- Hardness 文档负责“怎么执行与验收”。
- API/配置/架构文档负责“系统对外与对内事实定义”。
- Hardness 不能替代业务文档；任务完成后必须把结果回写到对应事实文档。

---

## 6. 语言与表述规范（强制）

## 6.1 必须使用可判定语句
- 推荐：`必须`、`应`、`禁止`、`通过条件为...`
- 禁止：`尽量`、`大概`、`应该没问题`、`基本完成`

## 6.2 必须给出量化标准
- 示例：`接口 P95 < 300ms`、`测试通过率 = 100%`

## 6.3 必须有“未完成项”章节
- 即使为空也必须写：`未完成项：无`

## 6.4 结论必须附证据定位
- 结论后附文件路径、命令、日志或截图位置

---

## 7. AI 执行协议（建议直接复制到系统提示词）

1. 先读取 hardness 文档并复述任务边界。
2. 若为 H2/H3，先输出执行计划，再进入编码。
3. 每完成一个任务单元，立即执行对应验证并记录证据。
4. 遇到不确定项，先标记阻塞，不得擅自假设关键业务规则。
5. 在长任务中按阶段做上下文摘要：已完成、未完成、风险、下一步。
6. 最终输出必须包含：完成项、未完成项、风险、证据索引。

---

## 8. 反模式清单（禁止）

- 只汇报“已完成”，不附验证证据
- 先大面积改代码，最后一次性跑测试
- 未拆分任务直接执行大需求
- 因上下文过长忽略早期约束
- 为了通过测试而硬编码/绕过真实逻辑
- 把“风格判断”当成验收结论（vibe-based）

---

## 9. 快速模板（可复制）

```md
# [任务ID] [任务名称] Hardness

## 元信息
- 分级：
- 负责人：
- 时间窗口：
- 关联：

## 目标与非目标
- 目标：
- 非目标：

## 输入约束
- 已知上下文：
- 强约束：
- 禁止事项：

## 任务分解
### T1
- 输入：
- 执行动作：
- 输出：
- 验收标准：
- 证据类型：

### T2
- 输入：
- 执行动作：
- 输出：
- 验收标准：
- 证据类型：

## 验证矩阵
| 验证项 | 方法 | 阈值/期望 | 结果证据 |
|---|---|---|---|

## 上下文与防漏策略
- 上下文预算：
- 分段策略：
- 压缩策略：
- 恢复策略：

## 风险与回退
- 风险：
- 触发条件：
- 回退步骤：

## 完成定义（DoD）
- [ ] 代码改动完成
- [ ] 验证矩阵执行
- [ ] 证据归档
- [ ] 未完成项列出

## 未完成项
- 无 / 列表
```

---

## 10. 参考来源（网络调研）

1. OpenAI Prompt Engineering（含编码任务的角色定义、工具调用、测试验证、上下文窗口规划）：  
https://developers.openai.com/api/docs/guides/prompt-engineering
2. OpenAI Evaluation Best Practices（目标-数据-指标-持续评估与反模式）：  
https://developers.openai.com/api/docs/guides/evaluation-best-practices
3. Anthropic Claude Code Best Practices（先探索-计划-实现、可验证反馈、上下文管理）：  
https://code.claude.com/docs/en/best-practices
4. Anthropic Prompting Best Practices（长上下文结构化、引用落地、XML 标签）：  
https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices#long-context-prompting
5. Lost in the Middle（长上下文中间信息利用衰减）：  
https://arxiv.org/abs/2307.03172
6. SWE-bench（真实 GitHub 问题作为编码能力评测场景）：  
https://arxiv.org/abs/2310.06770
7. OpenAI: Why SWE-bench Verified no longer measures frontier coding capabilities（基准污染与测试缺陷风险）：  
https://openai.com/index/why-we-no-longer-evaluate-swe-bench-verified/


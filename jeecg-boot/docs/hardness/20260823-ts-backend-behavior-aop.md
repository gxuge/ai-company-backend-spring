# 20260823 后端业务行为自动埋点 Hardness

## 元信息
- 任务 ID：20260823-ts-backend-behavior-aop
- 任务名称：后端业务行为自动埋点
- 分级：H2
- 负责人：Codex
- 时间窗口：2026-08-23
- 关联：`PLANS.md#20260823-ts-backend-behavior-aop`

## 目标与非目标
- 目标：至少覆盖点赞、收藏、反馈评论、角色/故事发布与生成五类成功事件。
- 目标：事务回滚事件数为 0，埋点异常导致业务失败数为 0。
- 目标：事件包含稳定的用户、资源、操作来源和发生时间。
- 非目标：不实现关注业务本身，不启用 Kafka，不调整推荐权重。

## 输入约束
- 已知上下文：现有 `TsBehaviorEventPublisher`、Kafka 开关、行为消息和业务 Service。
- 强约束：保持 Controller 响应和事务语义；用户 ID 只能来自登录态或后端查询结果。
- 禁止事项：禁止在业务事务提交前产生可消费事件；禁止因埋点失败抛出业务异常。

## 任务分解
### T1 统一埋点能力
- 输入：行为消息发布器和 Spring AOP/事务组件。
- 执行动作：新增方法注解、SpEL 切面、事件工厂和提交后协调器。
- 输出：可复用的后端可信行为采集组件。
- 验收标准：支持参数/返回值取值、条件表达式、事务提交后发布和异常隔离。
- 证据类型：单元测试、代码定位。

### T2 业务入口接入
- 输入：点赞、收藏、反馈评论、公开发布和 AI 生成 Service。
- 执行动作：在成功方法添加声明式埋点。
- 输出：五类后端可信事件。
- 验收标准：事件用户与资源归属正确，失败方法不发布。
- 证据类型：注解扫描、单元测试。

### T3 验证与文档
- 输入：本次代码及现有行为采集文档。
- 执行动作：执行定向测试、模块编译、差异和编码检查，更新 API 事实说明与变更记录。
- 输出：可复核交付证据。
- 验收标准：测试和编译通过，文档与实现一致。
- 证据类型：Maven 输出、`git diff --check`、编码检查。

## 验证矩阵
| 验证项 | 方法 | 阈值/期望 | 结果证据 |
|---|---|---|---|
| 表达式提取 | AOP 单元测试 | 用户、资源、条件均正确 | 测试输出 |
| 事务边界 | 协调器单元测试 | 提交前 0 次，提交后 1 次 | 测试输出 |
| 异常隔离 | 发布器抛异常测试 | 业务正常返回 | 测试输出 |
| 编译回归 | Maven compile | BUILD SUCCESS | 命令输出 |
| 编码与空白 | 字节/EOL 检查、diff check | 无异常 | 命令输出 |

## 上下文与防漏策略
- 上下文预算：入口盘点 25%，实现 45%，验证与文档 30%。
- 分段策略：先建通用能力，再逐类标注，最后统一验证。
- 压缩策略：保留事件清单、表达式、事务规则、未完成验证和风险。
- 恢复策略：从本文件和 `PLANS.md` 勾选状态继续，禁止重新推断已确认边界。

## 风险与回退
- 风险：DTO/VO 字段变化导致 SpEL 解析失败；信号为切面警告日志。
- 风险：同一业务被重复标注；通过只标注对外 Facade Service 避免生成链路重复。
- 回退步骤：删除新增组件和目标方法注解，恢复文档条目；无需回滚数据库。
- 回退验证：目标业务单测与模块编译通过，Kafka 无新增事件。

## 完成定义（DoD）
- [ ] 代码改动完成
- [ ] 验证矩阵执行
- [ ] 证据归档
- [ ] 未完成项列出

## 未完成项
- 待实现与验证。

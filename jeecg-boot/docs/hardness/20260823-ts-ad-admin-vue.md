# 20260823 海报与广告运营管理 Vue 页面 Hardness

## 元信息
- 任务 ID：`20260823-ts-ad-admin-vue`
- 任务名称：海报与广告运营管理 Vue 页面及菜单
- 分级：H2
- 负责人：AI
- 时间窗口：2026-08-23
- 关联：`20260823-ts-ad-center`、`jeecgboot-vue3`

## 目标与非目标
- 目标：交付三页签运营管理页面、三个编辑组件和一份菜单迁移。
- 目标：前端字段与现有广告管理接口一一对应。
- 目标：新增页面在暗色主题下不使用硬编码白色背景。
- 非目标：不开发用户端广告展示，不变更后端接口契约。

## 输入约束
- 已知上下文：现有探拾页面使用 `BasicTable/useListPage`、`BasicModal` 和 `BasicDrawer`。
- 强约束：API URL 只允许出现在 `adCenter.api.ts`；现有源码编码和换行不得改变。
- 禁止事项：不得修改会员配置、积分、活动和审核页面；不得创建前端静态路由绕过后台菜单。

## 任务分解
### T1 API 与数据配置
- 输入：`/sys/ts-ad-admin/**` 接口。
- 执行动作：新增接口类型、请求函数、表格列、查询表单和编辑表单。
- 输出：`adCenter.api.ts`、`adCenter.data.ts`。
- 验收标准：接口路径集中管理，字段覆盖广告位、内容、规则和统计。
- 证据类型：类型检查、路由扫描。

### T2 页面与组件
- 输入：现有探拾管理页面模式。
- 执行动作：新增主页、广告位弹窗、广告内容弹窗和规则抽屉。
- 输出：可操作的三个页签。
- 验收标准：CRUD、发布下线、规则保存和统计查询均有 UI 入口。
- 证据类型：Vue 转译、代码审查。

### T3 菜单与文档
- 输入：探拾父菜单和当前 V48 迁移。
- 执行动作：新增 V49 菜单并授权 admin，更新计划和变更记录。
- 输出：后台动态菜单可加载新页面。
- 验收标准：迁移可重复执行并包含回滚 SQL。
- 证据类型：SQL 检查、文档差异。

### T4 验证
- 输入：全部新增文件。
- 执行动作：执行 TypeScript/Vue 转译、定向 ESLint、编码和差异检查。
- 输出：验证记录。
- 验收标准：本次新增代码无类型、语法、编码或空白错误。
- 证据类型：命令输出。

## 验证矩阵
| 验证项 | 方法 | 阈值/期望 | 结果证据 |
|---|---|---|---|
| API 映射 | 静态扫描 | URL 集中且与后端一致 | 通过 |
| Vue 语法 | Vue SFC/构建检查 | 无新增错误 | 通过 |
| TypeScript | 定向类型检查 | 无新增错误 | 工具链启动失败，构建通过 |
| 暗色主题 | CSS 扫描 | 页面无硬编码白底 | 通过 |
| 菜单迁移 | SQL 检查 | 可重复执行且可回滚 | 通过 |
| 编码 | 字节与换行检查 | UTF-8 无 BOM/LF | 通过 |

## 上下文与防漏策略
- 上下文预算：API/数据 25%，组件与页面 45%，菜单与验证 30%。
- 分段策略：API、组件、主页、菜单、验证逐段执行。
- 压缩策略：保留字段映射、组件清单、未完成项和验证证据。
- 恢复策略：从本文件、`PLANS.md` 和 Git 状态恢复。

## 风险与回退
- 风险：全仓类型检查被历史问题阻塞；需区分本次文件与既有错误。
- 风险：上传地址为相对路径；展示时使用现有 URL 转换函数。
- 回退步骤：删除 `adCenter` 目录和 V49；删除对应管理员角色菜单授权。
- 回退验证：探拾原有菜单及页面正常加载。

## 完成定义
- [x] 代码改动完成
- [x] 验证矩阵执行
- [x] 证据归档
- [x] 未完成项列出

## 未完成项
- 未执行真实 MySQL 菜单迁移和登录态 HTTP 冒烟。
- `vue-tsc@1.8.27` 与当前 TypeScript/Node 组合不兼容，启动阶段失败；生产构建已通过。

## 验证证据
- `corepack pnpm exec eslint src/views/system/tanshi/adCenter --ext .ts,.vue`：通过。
- `corepack pnpm exec prettier --check src/views/system/tanshi/adCenter`：通过。
- `@vue/compiler-sfc`：4 个 Vue 文件解析通过。
- `corepack pnpm exec vite build`：9363 个模块转换完成，构建成功。
- 主题与接口扫描：无硬编码白底，接口 URL 仅位于 `adCenter.api.ts`。

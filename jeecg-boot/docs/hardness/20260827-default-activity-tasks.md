# 20260827-default-activity-tasks Hardness

## 元信息
- 任务 ID：20260827-default-activity-tasks
- 任务名称：活动条件触发接入与默认任务初始化
- 分级：H2
- 负责人：Codex
- 开始时间：2026-08-27
- 目标完成时间：2026-08-27
- 关联：活动中心需求清单

## 目标与非目标
- 目标：新增 3 个独立条件并接入 6 类业务成功入口。
- 目标：初始化 7 个自动发奖任务，重复执行 SQL 时新增记录数为 0。
- 目标：活动上报失败时主业务异常数增加量为 0。
- 非目标：不修改签到基础奖励，不清理历史任务配置。

## 输入约束
- 已知上下文：活动服务已支持每日/每周周期、行为幂等和自动发奖。
- 强约束：保持 Controller、响应结构、文件编码、BOM 和换行符不变。
- 禁止事项：禁止让活动上报异常回滚聊天、创建和生成业务。

## 任务分解
### T1 条件与上报器
- 输入：`TsActivityConditionType`、`ITsActivityService.reportProgress`。
- 执行动作：新增独立条件和事务提交后安全上报组件。
- 输出：枚举与上报组件。
- 验收标准：事务提交后调用；无事务时立即调用；异常不外抛。
- 证据类型：单元测试、编译输出。

### T2 业务触发
- 输入：聊天、角色、故事和图片生成成功路径。
- 执行动作：使用稳定业务 ID 上报对应条件。
- 输出：业务 Service 改动。
- 验收标准：仅成功路径调用；故事会话额外推进互动条件。
- 证据类型：代码路径检查、测试。

### T3 默认数据与前端
- 输入：活动任务表、Vue3 条件选项。
- 执行动作：新增项目外独立幂等补丁、保持数据库基线无默认任务并同步前端选项。
- 输出：独立 SQL 补丁、TypeScript。
- 验收标准：7 个任务参数与需求一致，周任务奖励为 20 星钻。
- 证据类型：SQL 静态检查、文件检查。

### T4 文档与验证
- 输入：项目文档规则和目标模块构建方式。
- 执行动作：同步 API、ADR、changelog，执行定向测试和编译。
- 输出：文档与验证记录。
- 验收标准：文档事实与代码一致，验证命令成功。
- 证据类型：Maven/JUnit 输出、文档路径。

## 验证矩阵
| 验证项 | 方法 | 阈值/期望 | 结果证据 |
|---|---|---|---|
| 条件合法性 | 枚举解析测试 | 3 个新值均合法 | 测试输出 |
| 事务语义 | 上报器单元测试 | 回滚不调用、提交调用 1 次 | 测试输出 |
| 异常隔离 | 上报器单元测试 | 下游异常不外抛 | 测试输出 |
| 默认数据 | SQL 静态检查 | 7 个任务且幂等 | SQL 文件 |
| 回归 | Maven 编译 | Reactor 成功 | 命令输出 |

## 上下文与防漏策略
- 上下文预算：入口分析 25%，编码 40%，SQL/文档 20%，验证 15%。
- 分段策略：先条件与触发，再默认数据与前端，最后验证。
- 压缩策略：保留目标、触发入口、默认参数、风险和未完成项。
- 恢复策略：从 `PLANS.md` 本任务章节和本文件恢复执行状态。

## 风险与回退
- 风险：进程在事务提交与回调执行之间退出会造成单次漏记。
- 触发条件：提交后应用进程异常终止。
- 回退步骤：停用默认任务并移除业务上报调用；数据库新增条件数据无需删除。
- 回退验证：主业务成功且不再产生新的活动进度事件。

## 完成定义（DoD）
- [x] 代码改动完成
- [x] 验证矩阵执行
- [x] 证据归档
- [x] 未完成项列出

## 未完成项
- 未在真实 MySQL 执行独立默认任务补丁。
- 未执行 Vue3 ESLint、Vite 构建和全量类型检查。

## 验证证据
- 主代码：`mvn -pl jeecg-module-system/jeecg-system-biz -am -DskipTests compile`
  执行成功，8 个 Reactor 模块全部成功。
- 测试编译：`mvn -pl jeecg-module-system/jeecg-system-biz -am -DskipTests test-compile`
  执行成功，目标模块 35 个测试源文件编译成功。
- 定向测试：JUnit Platform 独立执行上报器测试，输出
  `ACTIVITY_PROGRESS_REPORTER_TESTS succeeded=4 failed=0`。
- 数据：`D:\project_demo\ai-company-backend-spring\activity-default-tasks-patch.sql`
  包含 7 个任务，每个任务均使用 `NOT EXISTS` 防重复；完整数据库基线不含
  这 7 条任务数据。

# 20260827 角色详情页真实数据对接 Hardness

## 元信息
- 任务 ID：20260827-role-details-real-data
- 任务名称：角色详情页真实数据对接
- 分级：H2
- 负责人：Codex
- 开始时间：2026-08-27
- 目标完成时间：2026-08-27
- 关联：`GET /sys/ts-roles/public/detail`、Web `/pages/role-details`

## 目标与非目标
- 目标：公开角色详情一次返回页面所需的角色资料、作者信息和三项统计数据。
- 目标：Web 页面使用真实公开详情和用户收藏状态，移除对应演示数据。
- 非目标：不新增数据库结构，不实现当前没有业务接口的五个内容标签页。

## 输入约束
- 已知上下文：`ts_role_info`、`ts_role_public`、`ts_role_stat` 与用户收藏接口已存在。
- 强约束：公开浏览不得使用仅限角色所有者的 `/ts-roles/detail`。
- 强约束：保持 Web 405 rem 布局与现有视觉结构。
- 禁止事项：不得修改无关配置、数据库表或现有用户未提交改动。

## 任务分解
### T1：后端公开详情聚合
- 输入：公开角色 Mapper、VO 与现有 `ts_role_stat` 表。
- 执行动作：扩展 VO 字段并在详情 SQL 中关联角色统计。
- 输出：公开详情返回完整角色资料和三项统计。
- 验收标准：字段名与前端类型一致，空统计返回 `0`。
- 证据类型：目标文件差异、Maven 编译。

### T2：前端接口与页面状态
- 输入：公开详情接口与用户收藏接口。
- 执行动作：增加公开详情 API，接入详情加载、收藏状态和收藏切换。
- 输出：页面使用真实角色资料、统计和收藏状态。
- 验收标准：不再使用静态统计值；收藏失败会恢复状态并显示 Toast。
- 证据类型：TypeScript 检查、定向搜索。

### T3：文档与回归
- 输入：接口响应契约变化。
- 执行动作：更新 API 文档、变更记录并执行编码与差异检查。
- 输出：可追溯文档和验证结果。
- 验收标准：文档字段与代码一致，目标文件无空白或编码异常。
- 证据类型：检查命令输出、Git diff。

## 验证矩阵
| 验证项 | 方法 | 阈值/期望 | 结果证据 |
|---|---|---|---|
| SQL 字段映射 | 静态检查 | VO 与 SQL 别名完全一致 | Mapper/VO 差异 |
| 空统计兼容 | SQL `COALESCE` | 三项统计均返回非空数字 | Mapper SQL |
| 收藏状态 | 前端类型检查 | 查询与写操作无类型错误 | `bun run check:types` |
| 后端编译 | Maven 编译 | 目标模块成功 | Maven 输出 |
| 编码格式 | BOM/EOL 检查 | 保持各源文件原格式 | 编码检查输出 |

## 上下文与防漏策略
- 上下文预算：后端聚合、前端接入、验证文档三个阶段分别收敛。
- 分段策略：每阶段完成后立即执行目标检查并更新计划。
- 压缩策略：保留字段映射、未完成项、验证结果和风险。
- 恢复策略：重新读取本文件、目标 diff 和当前计划后继续。

## 风险与回退
- 风险：部署环境缺少 `ts_role_stat` 表。
- 触发条件：公开详情查询报告表不存在。
- 回退步骤：回滚 Mapper 关联和扩展字段，前端恢复仅展示核心字段。
- 风险：角色扩展字段为空导致旧设计区域无内容。
- 缓解：可选字段按真实数据条件渲染，不展示伪造文案。

## 完成定义（DoD）
- [x] 后端公开详情聚合完成
- [x] 前端真实数据和收藏状态接入完成
- [x] 验证矩阵执行并记录
- [x] 未完成项显式列出

## 未完成项
- 无。

## 验证结果
- SQL 字段映射：详情查询返回角色扩展资料，三项统计均使用 `COALESCE(..., 0)`。
- 权限边界：页面改用公开详情接口，未调用角色所有者专用详情接口。
- 收藏状态：查询、收藏和取消收藏均复用现有用户收藏接口。
- 后端：`mvn -pl jeecg-module-system/jeecg-system-biz -am -DskipTests compile`，Reactor 8 个模块全部成功。
- 前端：`bun run check:types` 通过。
- 差异与编码：前后端 `git diff --check` 通过，目标文件编码和换行符保持原格式。

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.TsUserBehaviorEventMapper">

    <insert id="insertEvent">
        INSERT INTO ts_user_behavior_event (
            event_id, event_type, event_version, user_id,
            session_id, resource_type, resource_id, page_path, platform, properties_json,
            occurred_at, received_at, created_at
        ) VALUES (
            #{eventId}, #{eventType}, #{eventVersion}, #{userId},
            #{sessionId}, #{resourceType}, #{resourceId}, #{pagePath}, #{platform}, #{propertiesJson},
            #{occurredAt}, #{receivedAt}, #{createdAt}
        )
    </insert>

</mapper>

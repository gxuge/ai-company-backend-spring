<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.TsUserVoiceConfigMapper">

    <!-- 按用户ID查询当前音色配置 -->
    <select id="selectByUserId" resultType="org.jeecg.modules.system.entity.TsUserVoiceConfig">
        SELECT *
        FROM ts_user_voice_config
        WHERE user_id = #{userId}
        ORDER BY updated_at DESC, id DESC
        LIMIT 1
    </select>

    <!-- 统计指定音色被用户配置引用数量 -->
    <select id="countBySelectedVoiceProfileId" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM ts_user_voice_config
        WHERE selected_voice_profile_id = #{selectedVoiceProfileId}
    </select>

</mapper>

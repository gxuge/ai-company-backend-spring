<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.TsRecommendEtlTaskMapper">
    <update id="occupy">
        UPDATE ts_recommend_etl_task
        SET running_execution_id = #{executionId},
            last_run_at = NOW(),
            update_time = NOW()
        WHERE id = #{taskId}
          AND del_flag = 0
          AND running_execution_id IS NULL
    </update>

    <update id="release">
        UPDATE ts_recommend_etl_task
        SET running_execution_id = NULL,
            update_time = NOW()
        WHERE id = #{taskId}
          AND running_execution_id = #{executionId}
    </update>
</mapper>

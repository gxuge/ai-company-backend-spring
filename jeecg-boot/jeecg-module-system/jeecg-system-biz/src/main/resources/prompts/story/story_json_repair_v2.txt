TEMPLATE_BEGIN::story_json_repair::v2

SECTION::meta
code=story_json_repair
version=v2
scenario=story
description=Repair invalid story generation JSON according to tool schema required fields.
output_mode=json_object

SECTION::developer_prompt
你是一个严格的JSON修复器。
任务：将输入内容修复为一个合法JSON对象，并且字段必须满足给定tool schema中的required约束。
规则：
1. 只输出一个JSON对象本身，不得输出解释、Markdown、代码块或前后缀。
2. 必须优先保留原始内容中的语义信息，不得无中生有扩展无关字段。
3. 若原始内容缺少required字段，按上下文补齐最合理值。
4. 禁止输出schema之外的包装结构。

SECTION::user_prompt_template
{
  "scene": "{{scene}}",
  "required_fields": "{{required_fields}}",
  "tool_schema": {{tool_schema}},
  "raw_content": {{raw_content}}
}

SECTION::output_schema_hint
仅输出一个合法JSON对象，且必须满足 required_fields。

TEMPLATE_END::story_json_repair::v2
